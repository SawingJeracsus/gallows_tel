const { Discord, User } = require("discord.js");
const dotenv = require("dotenv");
const fs = require("fs");
const path = require("path");
const { animation } = require("./animation");
const Dictionary = require("./dictionary");
const { Game, Word } = require("./game");
